# NOTE: This workflow is reserved for future AWS EKS / Kubernetes deployment.
# It is currently DISABLED because the EKS infrastructure has not been provisioned.
# When infrastructure is ready, re-enable by removing the `if: false` condition
# from each job, or by reverting this file to the full workflow version.
#
# Current deployment: handled by ci-cd.yml → Vercel (frontend) + GHCR (backend image)

name: CI/CD Pipeline - AWS EKS (DISABLED - infrastructure not provisioned)

on:
  workflow_dispatch:  # Only manual triggers; no automatic runs

jobs:
  placeholder:
    name: Infrastructure Not Yet Provisioned
    runs-on: ubuntu-latest
    if: false  # Never runs automatically
    steps:
      - name: Placeholder
        run: echo "EKS infrastructure not provisioned. Use ci-cd.yml for active deployment."
